"inside appointment index page"

<% @appointments.each do |appointment| %>
  <%= "link_to #{appointment.doctor.name}, doctor_path#{appointment.doctor}" %><br>  link_to "Profile", controller: "profiles", action: "show", id: @profile
<% end %>
