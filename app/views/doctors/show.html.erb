 <h1><%= @doctor.name %></h1>
 <h3><%= @doctor.department %></h3>

<% @doctor.appointments.each do |a| %>
  <%= a.appointment_datetime %><br>
  <%= "#{appointment.patient.name}" %>
  <%= link_to "#{appointment.patient.name}", doctor_path("#{appointment.doctor.id}") %>
<% end %>
